repeater("{{name}}")
{
    {{#each properties}}
    {{@key}} = {{this}};
    {{/each}}

    {{#each fields}}
    field("{{name}}"; Rec."{{sourceField}}")
    {
        {{#each properties}}
        {{@key}} = {{this}};
        {{/each}}
    }
    {{/each}}
}