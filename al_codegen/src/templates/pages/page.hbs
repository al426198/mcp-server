page {{id}} "{{name}}"
{
    {{#each properties}}
    {{@key}} = {{{this}}};
    {{/each}}

    layout
    {
        {{> (lookup properties "PageType")}}
    }
    {{#if actions}}

    actions
    {
        {{#each areas}}
        {{> area}}
        {{/each}}
    }
    {{/if}}
}