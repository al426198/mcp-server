page {{id}} "{{name}}"
{
    {{#if properties}}
    {{#each properties}}
    {{@key}} = {{{this}}};
    {{/each}}
    {{/if}}
    layout
    {
        {{> (lookup properties "PageType")}}
    }
    {{#if areas}}

    actions
    {
        {{#each areas}}
        {{> action-cluster}}
        {{/each}}
    }
    {{/if}}
}