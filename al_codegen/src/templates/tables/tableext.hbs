tableextension {{id}} "{{name}}" extends "{{target}}"
{
    {{#each properties}}
    {{@key}} = {{this}};
    {{/each}}
    {{#if fields}}
    fields
    {
        {{#each fields}}
        {{> field}}
        {{/each}}
    }
    {{/if}}
    {{#if keys}}
    
    keys
    {
        {{#each keys}}
        {{> key}}
        {{/each}}
    }
    {{/if}}
    {{#if fieldGroups}}

    fieldgroups 
    {
        {{#each fieldGroups}}
        {{> fieldgroup add=true}}
        {{/each}}
    }
    {{/if}}
}