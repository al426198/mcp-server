tableextension {{id}} "{{name}}" extends "{{target}}"
{
    {{#each properties}}
    {{@key}} = {{this}};
    {{/each}}
    {{#if showFields}}
    fields
    {
        {{#each fields}}
        {{> field}}
        {{/each}}

        {{#each modifyFields}}
        modify("{{name}}")
        {
            {{#each properties}}
            {{@key}} = {{this}};
            {{/each}}
        }
        {{/each}}
    }
    {{/if}}
    {{#if keys}}
    keys
    {
        {{#each keys}}
        {{> key}}
        {{/each}}
    }
    {{/if}}
    {{#if fieldGroups}}
    fieldgroups 
    {
        {{#each fieldGroups}}
        addlast({{type}}; {{#each fieldNames}}"{{this}}"{{#unless @last}}, {{/unless}}{{/each}})
        {
        }
        {{/each}}
    }
    {{/if}}
}