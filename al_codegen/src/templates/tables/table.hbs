table {{id}} "{{name}}"
{
    {{#each properties}}
    {{@key}} = {{this}};
    {{/each}}

    fields
    {
        {{#each fields}}
        {{> field}}
        {{/each}}
    }
    {{#if keys}}

    keys
    {
        {{#each keys}}
        {{> key}}
        {{/each}}
    }
    {{/if}}
    {{#if fieldGroups}}
    
    fieldgroups 
    {
        {{#each fieldGroups}}
        {{> fieldgroup}}
        {{/each}}
    }
    {{/if}}

    trigger OnInsert()
    begin

    end;

    trigger OnModify()
    begin

    end;

    trigger OnDelete()
    begin

    end;

    trigger OnRename()
    begin

    end;
}