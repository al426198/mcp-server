table {{id}} "{{name}}"
{

{{#each properties}}
{{this.key}} = {{this.value}};
{{/each}}

fields
{
{{#each fields}}
field({{id}}; "{{name}}"; {{type}})
{
{{#each properties}}
{{this.key}} = {{this.value}};
{{/each}}
}
{{/each}}
}

keys
{
{{#each keys}}
key({{name}}; {{#each fieldNames}}"{{this}}"{{#unless @last}}, {{/unless}}{{/each}})
{
{{#if clustered}}
Clustered = true;
{{/if}}
}
{{/each}}
}

{{#each fieldGroups}}
fieldgroup({{name}}; {{#each fieldNames}}"{{this}}"{{#unless @last}}, {{/unless}}{{/each}})
{
}
{{/each}}

trigger OnInsert()
begin

end;

trigger OnModify()
begin

end;

trigger OnDelete()
begin

end;

trigger OnRename()
begin

end;
}